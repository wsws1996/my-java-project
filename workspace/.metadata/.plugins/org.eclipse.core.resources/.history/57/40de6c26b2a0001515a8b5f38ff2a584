function makeYear() {
	var year = document.getElementById("year");
	alert("成功");
	for (var i = 1900; i <= new Date().getYear(); i++) {
		alert(i);
		var option = document.createElement("option");
		option.value = i;
		option.innerText = i;
		
		year.appendChild(option);
	}
}