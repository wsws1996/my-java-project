package cn.wang.demo;

import java.io.InputStream;
import java.io.OutputStream;
import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;

import cn.wang.utils.JdbcUtils;

public class Demo1 {

	/*
	 * create database day15; use day15; create table testclob { id int primary
	 * key auto_increment, resume text };
	 */

	public static void main(String[] args) {
		Connection connection = null;
		PreparedStatement preparedStatement = null;
		ResultSet resultSet = null;
		try {
			connection = JdbcUtils.getConnection();
			String sql = "insert into testclob(resume) values(?)";
			preparedStatement = connection.prepareStatement(sql);
			InputStream in= Demo1.class.getClassLoader().getResourceAsStream("1.txt");
			preparedStatement.setCharacterStream(1, reader, length);
		} catch (Exception e) {
			e.printStackTrace();
		} finally {

		}
	}

}
